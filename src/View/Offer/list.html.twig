{% extends 'layout.html.twig' %}
{% block title %}je recherche{% endblock %}
{% block content %}
    <div class="consult_list_offers_filter">
    </div>
    <div class= "consult_list_offers_container">
        <h1>Je cherche un </h1>
        <p>descriptif recherche</p>
        <form action="list.html.twig" method="post">
            <input type="radio" id="vente" name="transaction" value="vente">
            <label for="vente">Vente</label>
            <input type="radio" id="location" name="transaction" value="location">
            <label for="location">Location</label>
            <select name="department" id="department">
                {% for deparment in deparments %}
                <option value="default" selected>{{ department.name }}</option>
                {% endfor %}
            </select>
            <select name="city" id="city">
                <option value="default" selected>city</option>
                <option value="département-1">city_1</option>
            </select>
            <input type="button" value="go!" class="consult_list_offers_button">
        </form>
        <div class="consult_list_offers_card">
            <div class  = "consult_list_offers_image">
               <img src="https://via.placeholder.com/100" alt="product_1"></a>
            </div>
            <div class="card-body">
                <div class="tool_cartd_title">
                    <h3>Titre:</h3><p>
                    <h4>Prix:</h4><P></P>
                    <h4>Localisation:</h4><P></P>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
